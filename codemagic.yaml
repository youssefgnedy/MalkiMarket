workflows:
  ios-capacitor:
    name: iOS Capacitor Build

    environment:
      xcode: latest
      node: latest

   scripts:
  - npm install
  - npx cap sync ios
  - find . -name "*.xcodeproj"
  - find . -name "*.xcworkspace"
  - find . -name "App.xcodeproj"